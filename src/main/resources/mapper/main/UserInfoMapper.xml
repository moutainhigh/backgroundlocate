<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.backGroundLocate.mapper.main.UserInfoMapper">
    <resultMap id="BaseResultMap" type="com.backGroundLocate.entity.UserInfo">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="user_name" jdbcType="VARCHAR" property="userName" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="level" jdbcType="INTEGER" property="level" />
        <result column="dept_id" jdbcType="INTEGER" property="deptId" />
        <result column="dept_name" jdbcType="VARCHAR" property="deptName" />
        <result column="leader_id" jdbcType="INTEGER" property="leaderId" />
        <result column="mobile" jdbcType="VARCHAR" property="mobile" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="type" jdbcType="INTEGER" property="type" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_name,password,name,level,dept_id,dept_name,leader_id,mobile,status,type
    </sql>

    <select id="selectUserById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select <include refid="Base_Column_List" /> from t_user_info where id = #{id,jdbcType=INTEGER}
    </select>

    <select id="selectUser" resultMap="BaseResultMap" parameterType="com.backGroundLocate.entity.UserInfo">
        select <include refid="Base_Column_List" /> from t_user_info where 1=1

        <if test="userName!=null and userName!=''">
            and user_name = #{userName,jdbcType=VARCHAR}
        </if >
    </select>

    <select id="selectUserListByDept" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from t_user_info where dept_id = #{deptId,jdbcType=INTEGER}
    </select>


    <select id="selectUserIllegalNum" resultType="java.lang.Integer" parameterType="map">
      SELECT
            isnull(COUNT ( i.id ),0)
        FROM
            t_user_info u,
            t_illegal_info i
        WHERE
            i.unit_id = u.id
            AND i.illegal_time &gt;= #{ startTimestamp,jdbcType = INTEGER }
        AND i.illegal_time &lt;= #{ endTimestamp,jdbcType = INTEGER }
        AND u.id = #{ unitId,jdbcType = INTEGER }
    </select>
</mapper>