<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.backGroundLocate.mapper.main.BackgroundLocateUserNewestMapper">
    <resultMap id="BaseResultMap" type="com.backGroundLocate.entity.BackgroundLocateUserNewest">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="user_id" jdbcType="INTEGER" property="user_id" />
        <result column="address" jdbcType="VARCHAR" property="address" />
        <result column="longitude" jdbcType="VARCHAR" property="longitude" />
        <result column="latitude" jdbcType="VARCHAR" property="latitude" />
        <result column="longitude_latitude" jdbcType="VARCHAR" property="longitude_latitude" />
        <result column="upload_time" jdbcType="VARCHAR" property="upload_time" />
        <result column="times_tamp" jdbcType="INTEGER" property="times_tamp" />
    </resultMap>

    <insert id="saveLocationOfNewest" parameterType="com.backGroundLocate.entity.BackgroundLocateUserNewest" >
    insert into t_background_locate_user_newest(user_id,
                address,
                longitude,
                latitude,
                longitude_latitude,
                upload_time,
                times_tamp)
    values(#{user_id,jdbcType=INTEGER},
           #{address,jdbcType=VARCHAR},
           #{longitude,jdbcType=VARCHAR},
           #{latitude,jdbcType=VARCHAR},
           #{longitude_latitude,jdbcType=VARCHAR},
           #{upload_time,jdbcType=VARCHAR},
           #{times_tamp,jdbcType=INTEGER})


    </insert>

    <select id="selectUserLocationForNewest" resultType="java.util.Map" parameterType="map">
            SELECT  b.id, b.name,a.longitude_latitude as latlon  FROM t_background_locate_user_newest a,t_user_info b WHERE a.user_id = b.id  AND user_id IN (
                SELECT u.id FROM t_user_info u, t_department d  WHERE u.dept_id = d.id
                <if test="deptName!=null and deptName!=''">
                    AND (d.dept_name LIKE '%'+#{deptName,jdbcType=VARCHAR}+'%'  OR d.id = #{deptId,jdbcType=INTEGER} OR d.parent_id = #{parentId,jdbcType=INTEGER})
                </if >

                <if test="unitName!=null and unitName!=''">
                    AND u.name LIKE  '%'+#{unitName,jdbcType=VARCHAR}+'%'
                </if >
            )
	    </select>
    <delete id="deleteLocationOfNewest" parameterType="com.backGroundLocate.entity.BackgroundLocateUserNewest" >
        delete from t_background_locate_user_newest where user_id = #{user_id,jdbcType=INTEGER}
    </delete>
</mapper>