<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.backGroundLocate.mapper.main.DepartmentMapper">
    <resultMap id="BaseResultMap" type="com.backGroundLocate.entity.Department">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="dept_name" jdbcType="VARCHAR" property="deptName" />
        <result column="dept_type" jdbcType="INTEGER" property="deptType" />
        <result column="dept_level" jdbcType="INTEGER" property="deptLevel" />
        <result column="parent_id" jdbcType="INTEGER" property="parentId" />
        <result column="parent_name" jdbcType="VARCHAR" property="parentName" />
    </resultMap>

    <sql id="Base_Column_List">
        id,dept_name,dept_type,dept_level,parent_id,parent_name
    </sql>

    <select id="selectDepartmentList" resultMap="BaseResultMap" parameterType="com.backGroundLocate.entity.Department">
        select <include refid="Base_Column_List" /> from t_department where 1=1

        <if test="id != 0">
            and id = #{id,jdbcType=INTEGER}
        </if >
        <if test="parentId != 0">
            and parent_id = #{parentId,jdbcType=INTEGER}
        </if >

        <if test="deptType != 0">
            and dept_type = #{deptType,jdbcType=INTEGER}
        </if >

        <if test="deptName != null">
            and dept_name like '%'+#{deptName,jdbcType=VARCHAR}+'%'
        </if >
    </select>

    <select id="selectDepartment" resultMap="BaseResultMap" parameterType="com.backGroundLocate.entity.Department">
        select <include refid="Base_Column_List" /> from t_department where 1=1
        <if test="id != 0">
            and id = #{id,jdbcType=INTEGER}
        </if >
        <if test="parentId != 0">
            and parent_id = #{parentId,jdbcType=INTEGER}
        </if >

        <if test="deptType != 0">
            and dept_type = #{deptType,jdbcType=INTEGER}
        </if >

        <if test="deptName != null">
            and dept_name like '%'+#{deptName,jdbcType=VARCHAR}+'%'
        </if >
    </select>

    <select id="selectDepartmentByPrimary" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select <include refid="Base_Column_List" /> from t_department where 1=1

            and id = #{id,jdbcType=INTEGER}
    </select>

</mapper>